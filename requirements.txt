# Emind v2.0 大模型依赖
# Python >= 3.10

# 核心
torch>=2.0.0
numpy>=1.24.0
sentencepiece>=0.1.99

# Web 服务
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.2.0
jinja2>=3.1.0
sse-starlette>=1.8.0

# 推理后端
transformers>=4.36.0
accelerate>=0.25.0
requests>=2.31.0
pydantic>=2.10.0
httpx>=0.25.0

# 蒸馏仪表盘 (zhengliu)
rich>=13.0.0

# 可选 — 监控 (训练用)
# wandb>=0.16.0
# tensorboard>=2.14.0

# 可选 — vLLM
# vllm>=0.3.0

# 可选 — Ollama
# ollama>=0.1.0

# 可选 — llama.cpp
# llama-cpp-python>=0.2.0

# 可选 — 评测
# scikit-learn>=1.3.0

# 可选 — 评测
# scikit-learn>=1.3.0
